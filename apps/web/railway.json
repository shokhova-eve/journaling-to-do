{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npm run build --workspace=@journaling/shared && npm run build --workspace=@journaling/web"
  },
  "deploy": {
    "startCommand": "npm run start --workspace=@journaling/web"
  }
}
