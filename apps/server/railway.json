{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npm run build --workspace=@journaling/shared && npm run build --workspace=@journaling/server"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema apps/server/prisma/schema.prisma && npm run start --workspace=@journaling/server"
  }
}
